name: Deploy Practice App
on:
  push:
    branches: [main]
    paths: ['practice-app/**']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: practice-app/pnpm-lock.yaml
      - run: cd practice-app && pnpm install --frozen-lockfile
      - run: cd practice-app && pnpm build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: practice-app/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
